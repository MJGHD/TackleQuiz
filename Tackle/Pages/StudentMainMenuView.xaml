<UserControl x:Class="Tackle.Pages.StudentMainMenuView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:s="https://github.com/canton7/Stylet"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Tackle.Pages"
             mc:Ignorable="d" 
             d:DesignHeight="550" d:DesignWidth="900">
    <Grid>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <Button Command="{s:Action}" Margin="0,0,0,5" FontSize="24">View Homework</Button>
            <Button Command="{s:Action}" Margin="0,0,0,5" FontSize="24">View Public Quizzes</Button>
            <Button Command="{s:Action}" Margin="0,0,0,5" FontSize="24">View Quiz History</Button>
            <Button Command="{s:Action LoadTest}" Margin="0,0,0,5" FontSize="24">Load Test Quiz</Button>
        </StackPanel>
    </Grid>
</UserControl>
