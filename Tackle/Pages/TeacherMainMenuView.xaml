<UserControl x:Class="Tackle.Pages.TeacherMainMenuView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:s="https://github.com/canton7/Stylet"
             xmlns:local="clr-namespace:Tackle.Pages"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
            <Button Command="{s:Action CreateQuiz}" Margin="0,0,0,5" FontSize="24">Create Quiz</Button>
            <Button Command="{s:Action DraftList}" Margin="0,0,0,5" FontSize="24">View Draft Quizzes</Button>
            <Button Margin="0,0,0,5" FontSize="24">View Currently Set Quizzes</Button>
            <Button Command="{s:Action ViewPublicQuizzes}" Margin="0,0,0,5" FontSize="24">View Public Quizzes</Button>
            <Button Margin="0,0,0,5" FontSize="24">Mark Quizzes</Button>
            <Button Margin="0,0,0,5" FontSize="24" Command="{s:Action ManageClasses}">Manage Classes</Button>
        </StackPanel>
    </Grid>
</UserControl>
