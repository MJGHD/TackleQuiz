<UserControl x:Class="Tackle.Pages.TeacherMainMenuView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:s="https://github.com/canton7/Stylet"
             xmlns:local="clr-namespace:Tackle.Pages"
             mc:Ignorable="d" 
             d:DesignHeight="550" d:DesignWidth="900">
    <Grid>
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
            <Button Command="{s:Action}" Margin="0,0,0,5" FontSize="24">Create Quiz</Button>
            <Button Command="{s:Action}" Margin="0,0,0,5" FontSize="24">View Draft Quizzes</Button>
            <Button Command="{s:Action}" Margin="0,0,0,5" FontSize="24">View Public Quizzes</Button>
            <Button Command="{s:Action}" Margin="0,0,0,5" FontSize="24">Mark Quizzes</Button>
            <Button Command="{s:Action}" Margin="0,0,0,5" FontSize="24">Manage Classes</Button>
        </StackPanel>
        
    </Grid>
</UserControl>
