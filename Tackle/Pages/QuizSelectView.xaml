<Window x:Class="Tackle.Pages.QuizSelectView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Tackle.Pages"
        xmlns:s="https://github.com/canton7/Stylet"
        mc:Ignorable="d"
        Title="Quiz selected..." Height="150" Width="300">
    <Grid>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Height="20">
            <Button Command="{s:Action TakeQuiz}" Margin="0,0,5,0">Take quiz</Button>
            <Button Command="{s:Action Edit}" Margin="0,0,5,0">Edit</Button>
            <Button Command="{s:Action SendToClass}" Margin="0,0,5,0">Send to class</Button>
            <Button Command="{s:Action Delete}" Margin="0,0,5,0">Delete</Button>
        </StackPanel>
    </Grid>
</Window>
